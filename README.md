# Pixida Task for Cloud Developers

## Requirements
- Python 3.9
- Django 4.1
- PostgreSQL 14

## Running the project
You can run the project locally with the command
    
    `docker-compose up --build`
 